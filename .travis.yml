language: ruby
sudo: false
rvm:
 - 2.2
install:
 - bundle install
 - npm install
 - npm install -g firebase-tools
script:
 - npm run lint
 - npm run build
 - firebase deploy --token "${FIREBASE_TOKEN}"