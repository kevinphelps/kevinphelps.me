language: generic
sudo: false
before_install:
 - rvm install 2.2
 - nvm install 4.1
install:
 - bundle install
 - npm install
 - npm install -g firebase-tools
script:
 - npm run lint
 - npm run build
after_success:
 - firebase deploy --token "${FIREBASE_TOKEN}"